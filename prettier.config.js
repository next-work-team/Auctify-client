module.exports = {
  semi: true, // 세미콜론을 항상 사용
  singleQuote: true, // 문자열에 싱글 따옴표 사용
  tabWidth: 2, // 탭 너비는 2칸
  useTabs: false, // 탭 대신 공백을 사용
  trailingComma: 'all', // 객체, 배열, 함수 등의 마지막 항목에 후행 쉼표 추가
  bracketSpacing: true, // 객체의 중괄호 안에 공백을 추가
  jsxBracketSameLine: false, // JSX에서 `>`가 마지막 줄에 올 수 있도록 설정
  arrowParens: 'always', // 화살표 함수의 매개변수 괄호를 항상 사용
  printWidth: 80, // 한 줄의 최대 길이는 80자
  endOfLine: 'lf', // 라인 종료는 `\n` (Unix 스타일)
};
